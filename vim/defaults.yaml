vim:
  pkgs:
    - vim
  config_file: /etc/vimrc
  config_src: salt://vim/files/vimrc
  config_user: root
  config_group: root
  config_mode: '644'
  share_dir: /usr/share/vim/vimfiles
  plugin_dir: plugged
  plugin_manager_file: autoload/plug.vim
  users: {}

vim_config: {}
